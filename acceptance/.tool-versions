# Toolchain for the acceptance suite, for asdf / mise users.
#   mise install      (or)      asdf install
# CI pins the same major version via actions/setup-java (java-version: 17).
java temurin-17
maven 3.9.9
